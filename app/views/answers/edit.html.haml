%h1= @question.body
= simple_form_for @answer do |form|
  = form.association :choice, collection: @question.choices, as: :radio_buttons, label_method: :body, checked: @answer.choice_id, label: false
  = form.submit "Next", class: "button float-right"
