%h1= @question.body

%table
  -@question.choices.each do |choice|
    %tr
      %td= choice.body
      %td= link_to "Edit Choice", edit_choice_path(choice), class: 'button small m0'

=link_to "Add Choice", new_question_choice_path(@question), class: 'button'
